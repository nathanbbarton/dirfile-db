{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "types": ["node", "jest"],
    },
    "include": ["src/**/*", "tests/**/*"],
    "exclude": [ // Exclude directories
        "node_modules",
        "dist",
    ]
}
